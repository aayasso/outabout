<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ backup/transfer rules. The app's SharedPreferences hold a live
     Supabase session token; restoring it onto another device would hand that
     device a signed-in session. Nothing here is worth backing up - all durable
     state lives server-side against the account. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
